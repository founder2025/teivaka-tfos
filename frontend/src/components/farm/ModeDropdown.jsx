/**
 * ModeDropdown — retired. Solo/Growth/Commercial "mode" was removed from the
 * platform (one unified experience). This is a no-op kept only so existing
 * imports keep resolving; remove the imports + this file in a later cleanup.
 */
export default function ModeDropdown() {
  return null;
}
